# Questline — product repo

# secrets / env (never commit; clients ship only URL + anon key via .env.example)
.env
.env.*
!.env.example

# dependencies / build (per-client)
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo

# android
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.keystore
*.jks

# ios
ios/build/
ios/Pods/
ios/**/*.xcuserstate

# os / editor
.DS_Store
Thumbs.db

# agent machine-local state
.claude/
